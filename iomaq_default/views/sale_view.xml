<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>
        <!-- ocultar boton confirmar presupuesto solo a los vendedores -->
        <record id="view_order_form_id" model="ir.ui.view">
            <field name="name">IOMAQ_DEFAULT: ocultar Confirmar Venta</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="groups_id"
                   eval="[(4, ref('group_salesman_users'))]"/>

            <field name="arch" type="xml">

                <xpath expr="//button[@name='action_confirm'][1]"
                       position="attributes">
                    <attribute name="invisible">1</attribute>
                </xpath>

                <xpath expr="//button[@name='action_confirm'][2]"
                       position="attributes">
                    <attribute name="invisible">1</attribute>
                </xpath>

            </field>
        </record>

    </data>
</openerp>
